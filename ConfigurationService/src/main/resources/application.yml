server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: ConfigurationService
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config